# 대시보드 프로젝트 규칙

> 새 대시보드를 만들거나 고칠 때 이 문서를 AI에게 먼저 붙여넣으세요.
> 마지막 수정: 2026-08-15 · 대시보드 6개 완성 시점

---

## 0. 문서 두 개를 항상 같이 봅니다

| 문서 | 담는 것 |
|---|---|
| `RULES.md` (이 문서) | **어떻게 만들어야 하는가** — 규칙과 원칙 |
| `SCHEMA.md` | **지금 어떻게 되어 있는가** — 표·뷰의 실제 구조 |

**테이블이나 뷰를 바꾸면 그 작업의 마지막 단계로 `SCHEMA.md` 를 고칩니다.**
칼럼을 추가·삭제했을 때, 뷰를 새로 만들거나 계산식을 바꿨을 때,
정해진 값 목록(check 제약)을 늘렸을 때가 해당합니다.

`schema-sql.txt` 를 SQL Editor에서 실행하면 현재 구조가 그대로 나옵니다.
그 결과를 보고 `SCHEMA.md` 를 맞추면 됩니다.

**이 문서가 실제와 어긋나면 다음 작업이 어긋납니다.**
이미 있는 칼럼을 또 만들거나, 뷰가 계산하는 값을 저장하려 들게 됩니다.
사람이든 AI든 `SCHEMA.md` 를 사실이라고 믿고 작업하기 때문입니다.

## 1. 이 프로젝트가 뭔지

사내 대시보드 6개를 웹페이지로 만들어 링크로 공유합니다.
숫자는 Supabase에서 고치면 웹페이지에 자동 반영됩니다.

```
GitHub (my-dashboard)  →  코드 보관
Supabase               →  숫자 보관
Vercel                 →  웹페이지로 공개 (GitHub에 올리면 1~2분 뒤 자동 반영)
```

## 2. 폴더와 주소

| 파일 위치 | 접속 주소 | 내용 |
|---|---|---|
| `index.html` | `/` | 6개 링크 + 영역별 핵심 숫자 |
| `sales/index.html` | `/sales` | 영업 |
| `marketing/index.html` | `/marketing` | 마케팅 |
| `customer/index.html` | `/customer` | 고객 |
| `pl/index.html` | `/pl` | 손익 |
| `hr/index.html` | `/hr` | 인사 |
| `exec/index.html` | `/exec` | 경영 (신호등 + 현금) |
| `RULES.md` | — | 이 문서 (웹에는 안 보임) |

**파일 이름은 항상 `index.html`.** 바뀌는 건 폴더 이름뿐입니다.

## 3. 절대 지켜야 할 것

- **파일 하나로 완성.** HTML·CSS·JS를 한 `index.html`에 다 넣습니다.
- **빌드·설치 과정 없음.** GitHub에 올리면 그걸로 끝이어야 합니다.
- **차트 라이브러리 금지.** 그래프는 순수 SVG 또는 HTML/CSS로 직접 그립니다.
- **`type="module"` 금지.** Supabase는 아래 방식으로만 불러옵니다.

```html
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script>
  const SUPABASE_URL = 'https://abgpkybrwtpzflkchchu.supabase.co';
  const SUPABASE_KEY = 'sb_publishable_4LUZJYwgyDYPb-EzzpDHRw_FwEUvJ1e';

  const { createClient } = supabase;
  const db = createClient(SUPABASE_URL, SUPABASE_KEY, {
    auth: { persistSession: false, autoRefreshToken: false }
  });
</script>
```

## 4. 이미 한 번 데인 것들 — 반복 금지

**변수 이름을 `URL`로 쓰면 안 됩니다.**
`const URL = '...'` 은 브라우저 내장 기능을 덮어써서 Supabase가 주소를 해석하지 못합니다.
증상: `Invalid supabaseUrl: Provided URL is malformed`
→ 반드시 `SUPABASE_URL`, `SUPABASE_KEY`로 씁니다.

**`persistSession: false`를 넣습니다.** 없으면 브라우저가 저장소 접근을 막아 경고가 쌓입니다.

**함수 안에서 화면 설정값과 같은 이름을 쓰면 안 됩니다.**
깔때기 함수 안에 `const unit = 계약단가` 를 만들었더니 화면 단위 설정값 `unit` 을 가려서
단위가 `undefined` 로 표시됐습니다. 지역 변수는 `unitPrice` 처럼 다른 이름을 씁니다.
같은 이유로 `unit`, `year`, `month`, `DATA` 는 함수 안에서 다시 선언하지 않습니다.

**RLS를 안 열면 데이터가 안 옵니다.** 테이블·뷰를 만들 때마다 읽기 정책을 함께 만듭니다.

**큰 수를 곱하면 정수 범위를 넘칩니다.**
증상: `ERROR: 22003: integer out of range`
→ 나누기가 뒤에 있어도 곱셈이 먼저 실행되므로 `::double precision`을 곱하기 **전에** 붙입니다.

**GitHub에서 덮어쓸 땐 해당 폴더 안으로 들어간 뒤 업로드합니다.**
루트에서 올리면 새 파일이 하나 더 생길 뿐 교체되지 않습니다.

**파일 이름은 반드시 `index.html` 이어야 합니다.** 세 가지 이유입니다.

| 파일 위치 | 접속 주소 |
|---|---|
| `sales/index.html` | `/sales` |
| `sales/sales-index.html` | `/sales/sales-index.html` — `/sales` 는 404 |

첫째, **Vercel은 폴더 안의 `index.html` 을 그 폴더 주소의 대표 화면으로 봅니다.**
다른 이름이면 파일명까지 주소에 붙고, `/sales` 로 들어가면 404가 납니다.

둘째, **링크가 전부 깨집니다.** 랜딩 카드는 `href="/sales"`,
각 화면의 뒤로가기는 `href="/"` 로 되어 있습니다.

셋째, **교체가 아니라 추가가 됩니다.** 이미 있는 `index.html` 은 그대로 남고
새 파일이 하나 더 생겨, 옛날 화면이 계속 보입니다.

**그래서 다운로드한 파일은 올리기 직전에 `index` 로 바꿉니다.**
받자마자 바꾸지 말고 **한 번에 하나씩** 처리하세요.
일곱 개를 한꺼번에 받아 전부 `index` 로 바꾸면 어느 게 어느 영역인지 구분이 안 됩니다.

**다음 파일을 받기 전에 다운로드 폴더를 비웁니다.** 안 그러면 `index (1).html` 이 되어 교체가 안 됩니다.

**화면이 예전 그대로라면 파일이 안 올라간 것입니다.**
숫자가 이상하게 보일 때 코드를 의심하기 전에, 그 폴더의 `index.html` 을 열어
최근 수정 시각부터 확인하세요.

**새 폴더를 만들 때는 Upload가 아니라 Create new file.** 빈 폴더는 업로드로 만들 수 없습니다.

## 5. Supabase 규칙

**테이블 이름** — `영역_내용` 형식. 예: `sales_monthly`, `marketing_channel`

**금액은 원 단위로 저장합니다.** 화면에서 만원·백만원·원으로 바꿔 보여주되 DB에는 항상 원으로 저장합니다.

**저장은 가장 잘게, 표시는 필요한 만큼.** 만원으로 저장하면 8,470,500원의 500원이 영영 사라지지만,
원으로 저장하면 만원이든 백만원이든 언제든 만들어낼 수 있습니다.
작은 계약이나 미수금 대조처럼 원 단위가 필요한 순간이 반드시 옵니다.

**CPC·CPA·CAC처럼 단가 성격의 값은 화면에서 항상 '원'으로** 표시합니다. 단위 버튼과 무관합니다.

**단위를 어디에 쓸지는 숫자의 개수로 정합니다.**
값이 여러 개 나란히 놓이는 표·그래프는 **머리말이나 안내문에 한 번만** (`계약금액 (만원)`),
단독으로 나오는 카드 값은 **숫자에 직접** (`8,470만원`) 붙입니다.
표기 단위는 화면 상단 드롭다운을 따릅니다. 저장 단위(원)와는 별개입니다.
표의 모든 값에 단위를 붙이면 자릿수 비교가 어려워집니다.

**순서용 번호는 사람이 넣지 않습니다.**
`sort_order` 처럼 순서만 나타내는 숫자는 저장하지 않고, 글자에서 뷰가 계산합니다.
`견적`을 보면 사람은 바로 알지만 `2`는 매번 조견표를 확인해야 합니다.
읽기 쉬운 값을 사람이 넣고, 정렬용 숫자는 시스템이 만듭니다.

| 테이블 | 사람이 넣는 것 | 뷰가 만드는 것 |
|---|---|---|
| `sales_pipeline` | `stage` (문의/견적/협상/계약) | `sort_order` 1~4 |
| `hr_tenure` | `bucket` (3년 이상 …) | `sort_order` 1~4 |
| `customer_cohort` | 가입 연·월 + 확인 연·월 | `m_index` (두 날짜의 차) |
| `hr_overtime` | `hours` (실제 시간) | `bucket` (0h / 1-10 / …) |

**세는 값과 구간도 마찬가지입니다.**
`m_index`를 넣으려면 "3월에서 7월이니 4"를 매번 계산해야 하고,
`bucket`을 고르려면 "25시간이니 21-30"을 판단해야 합니다.
**본 그대로 옮겨 적을 수 있는 값**을 넣게 하고, 나머지는 뷰가 만듭니다.

구간 기준을 바꾸고 싶을 때도 뷰만 고치면 됩니다. 과거 데이터를 다시 넣지 않아도 됩니다.

`sort_order`가 여전히 필요한 곳은 **글자에서 순서를 유추할 수 없는 경우**뿐입니다.

**시간은 `year`, `month` 숫자 칼럼으로.**
`sort_order`로 순번을 매기지 않습니다. 중간에 한 줄 끼워 넣을 때마다 뒤 번호를 전부 고쳐야 하기 때문입니다.
`sort_order`는 **순서에 의미가 있지만 시간이 아닌 것**에만 씁니다. 예: 영업 단계, 근속 구간

**아직 지나지 않은 기간은 비워둡니다(null).** 0을 넣으면 실적이 0인 것과 구분이 안 됩니다.

**새 테이블·뷰마다 읽기 권한을 엽니다.**

```sql
alter table 테이블명 enable row level security;
create policy "read" on 테이블명 for select to anon using (true);
```

**키는 공개돼도 안전합니다.** `sb_publishable_...` 키는 읽기 전용입니다.

## 6. 같은 숫자를 두 곳에 저장하지 않습니다

이 프로젝트에서 가장 중요한 규칙입니다.

**하나의 사실은 한 곳에만 저장하고, 다른 곳은 계산해서 가져옵니다.** 두 곳에 저장하면 언젠가 반드시 어긋나고, 그때 어느 쪽이 맞는지 알 수 없게 됩니다.

**계산되는 값은 저장하지 않습니다.** ROAS, 영업이익, 달성률, 전환율, LTV 모두 코드나 뷰에서 계산합니다.

**원본 데이터가 겹칠 때는 뷰(View)를 씁니다.** 뷰는 저장하지 않고 볼 때마다 원천에서 계산해 오는 통로입니다.

| 사실 | 원천 | 가져다 쓰는 곳 |
|---|---|---|
| 월 매출 | `sales_monthly` | `pl_monthly_v` |
| 월 광고비 | `marketing_channel` | `pl_cost_v` |
| 인건비 총액 | `pl_cost` | 인사 화면 |
| LTV | `customer_ltv_v` | 마케팅·고객 화면 |
| 광고·직접 문의 건수 | `marketing_channel` | `sales_pipeline_v` |

```sql
create or replace view 이름 with (security_invoker = on) as
select ...;
```

`security_invoker = on`을 빼면 RLS가 적용되지 않습니다.

**뷰를 만들거나 고친 뒤에는 `SCHEMA.md` 의 「뷰가 하는 일」 표를 갱신합니다.**
어떤 표를 합치는지, 무엇을 계산하는지가 적혀 있어야
다음 작업에서 같은 값을 또 저장하는 일이 없습니다.

**부서별 인건비처럼 배분이 필요한 경우, 금액이 아니라 비중(%)을 저장합니다.** 총액에 비중을 곱하면 합계가 항상 맞습니다.

**예외** — 회계 매출과 영업 매출처럼 실제로 다른 값이어야 하는 경우가 있습니다. 그때는 같은 이름으로 두 곳에 저장하지 말고 **다른 이름으로 둘 다 저장**하고, 그 차이 자체를 지표로 봅니다.

## 7. 디자인 규칙

- 흰 배경, 폰트는 Pretendard CDN
- 카드·패널: 테두리 `#E7EAF0`, 모서리 14px / 회색 글자 `#64748B`
- 휴대폰에서 안 깨져야 합니다

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.min.css">
```

**영역마다 포인트 색은 하나씩. 랜딩 페이지 카드 색이 기준입니다.** 색을 새로 고르지 말고 아래 표에서 가져다 씁니다.

| 영역 | 포인트 색 |
|---|---|
| 영업 `/sales` | `#1D4ED8` 파랑 |
| 마케팅 `/marketing` | `#6D45D9` 보라 |
| 고객 `/customer` | `#0E9E6E` 초록 |
| 손익 `/pl` | `#C2710A` 주황 |
| 인사 `/hr` | `#C43B69` 자홍 |
| 경영 `/exec` | `#41576B` 회청 |

강약이 필요하면 다른 색을 더하지 말고 **그 색의 농도**로 구분합니다.
`/exec` 신호등만 예외로 영역 6색을 그대로 씁니다.

**증감·달성 표시는 색만으로 하지 않습니다.** 포인트 색 배경 위에서는 회색 글자가 보이지 않고, 색을 구분하기 어려운 사람도 있습니다. **채움과 테두리로도 구분**되게 만듭니다.

| | 흰 배경 위 | 포인트 색 배경 위 |
|---|---|---|
| 좋음 | 포인트 색 채움 + 흰 글씨 | 흰색 채움 + 진한 포인트 색 글씨 |
| 나쁨 | 흰 배경 + 회색 테두리 | 투명 + 흰 테두리 + 흰 글씨 |
| 중립 | 연회색 채움 | 반투명 흰색 채움 |

**경고색은 따로 둡니다.** 기본은 `#B45309`, **포인트 색이 주황 계열인 손익 영역에서는 `#C0392B`**를 씁니다.
신호등 상태색은 정상 `#0E9E6E` · 주의 `#D97706` · 경고 `#C0392B`.

**조작 도구는 선택지 수에 따라 나눕니다.**

- 연도·월·단위처럼 **선택지가 많거나 늘어나는 것** → 드롭다운
- 기간(당월/누적), 표시 개수(5·10·20·전체)처럼 **선택지가 2~4개** → 버튼

## 8. 코드에 개수를 박아두지 않기

담당자가 3명이든 20명이든, 부서가 3개든 10개든 **화면이 데이터에 맞춰 늘어나야** 합니다.
Supabase에서 행을 추가하는 것만으로 반영되어야 하고, 그때 코드를 고쳐야 한다면 잘못 만든 것입니다.

## 9. 화면을 만들 때 지키는 것

**연도·월 선택은 모든 대시보드에 넣습니다.** 데이터가 있는 기간만 목록에 나오게 하고, 기본은 가장 최근입니다.

**영역마다 최신 달이 다를 수 있습니다.** 마케팅은 진행 중인 달이 있고 손익은 마감된 달까지입니다. 화면 위에 **기준 시점을 명시**합니다.

**숫자 옆에 판단 기준을 같이 둡니다.** "ROAS 464%"만으로는 좋은지 알 수 없습니다. 목표, 전월, 전년, 손익분기 같은 비교 대상을 함께 보여줍니다.

**낮을수록 좋은 지표는 달성률로 표시하지 않습니다.**
인건비 비중을 `목표 ÷ 실적`으로 계산하면 195% 같은 값이 나와 오해를 부릅니다.
→ **실제 값을 그대로 크게 보여주고**, 막대는 기준선 대비 채움으로, 설명에 "낮을수록 좋음 · 기준 35%"를 붙입니다.

**데이터를 못 불러오면** 화면을 비우지 말고 주황색 안내줄과 함께 예시 값을 보여주거나, 최소한 원인을 안내합니다.

**검산 장치를 넣습니다.** 현금은 `월초 + 유입 − 유출 = 월말`, 부서는 `비중 합 = 100%`, 인원은 `부서 합 = 전사`. 안 맞으면 화면에 경고를 띄웁니다.

**두 화면의 숫자가 다르면 억지로 맞추지 말고, 왜 다른지 먼저 봅니다.**
마케팅 전환 493건과 영업 문의 1,036건이 달랐던 것은 오류가 아니라
**광고를 거치지 않은 유입이 따로 있었기 때문**이었습니다.
`sales_pipeline` 에 `source`(광고 / 직접 문의 / 소개·추천 / 영업 발굴)를 넣어 나누자
광고 의존도와 경로별 계약률이라는 새 지표가 생겼습니다.
숫자가 안 맞을 때는 **정의를 통일하기 전에 쪼갤 수 있는지** 먼저 검토하세요.

## 10. 지금 들어 있는 숫자는 전부 샘플입니다

화면 확인용으로 만든 가짜 숫자이고, **일부는 SQL이 자동 생성**합니다.
`sales_rank`, `marketing_channel`, `customer_cohort`, `hr_team`이 그렇습니다.

**실제 숫자를 손으로 넣기 시작한 뒤에는 그 SQL을 다시 실행하면 안 됩니다.**
첫 줄이 `drop table`이라 입력한 내용이 전부 지워집니다.

**실제 데이터로 넘어갈 때 할 일**

1. 지금 데이터를 CSV로 내보내 보관 (11번 참고)
2. 자동 생성 SQL은 더 이상 쓰지 않기로 하고 따로 보관
3. 실제 실적을 넣는 방식 결정 (직접 입력 / CSV 가져오기)

**숫자를 크게 고치기 전에는 CSV로 한 번 내보내 두세요.** 되돌릴 방법이 그것뿐입니다.

**규모가 서로 맞는지 확인하세요.** 매출 8,470만원 회사에 인원 24명을 넣으면 인건비만 매출의 85%가 되어 이익이 나올 수 없습니다. 지금 샘플은 **5명 규모**로 맞춰져 있습니다.

## 11. 데이터 넣고 빼기

### 넣기 — 엑셀 양식 → CSV → Import

`입력양식.xlsx` 에 시트 17개가 테이블별로 나뉘어 있습니다.
각 시트는 **1행이 제목줄, 2행이 빨간 예시줄, 3행부터 입력란**입니다.

1. 해당 시트에 그 달 줄을 **새로 추가**합니다 (기존 줄은 건드리지 않음)
2. **2행 빨간 예시줄을 지웁니다** — CSV의 첫 줄이 반드시 제목줄이어야 합니다
3. 파일 → 다른 이름으로 저장 → **CSV UTF-8** (지금 보고 있는 시트 하나만 저장됩니다)
4. Supabase → Table Editor → 해당 테이블 → 오른쪽 위 **초록색 `Insert` 버튼** → **Import data from CSV**

**시트마다 파일을 따로 만들어 하나씩 올립니다.** CSV는 표 하나만 담는 형식이라
여러 시트를 한 파일에 넣을 수 없습니다. 시트 하나 = 파일 하나 = 테이블 하나입니다.

**Import 는 점 세 개 메뉴가 아니라 `Insert` 버튼 안에 있습니다.**
점 세 개에는 Enable Realtime, View API docs, Enable Index Advisor 만 있습니다.

**금액은 원 단위, 쉼표 없이.** `84,700,000` 이 아니라 `84700000` 입니다.
엑셀에서 쉼표는 보기용 서식이지만 CSV로 저장하면 실제 글자로 들어가 오류가 납니다.

**파란 제목줄을 바꾸지 마세요.** `revenue` 를 `매출` 로 고치면 올라가지 않습니다.

**모르는 값은 빈칸으로.** `0` 을 넣으면 실적이 0인 것으로 계산됩니다.

**넣지 않아도 되는 것** — 손익의 매출과 광고선전비, 인사의 인건비, 영업 경로의 광고·직접 문의.
모두 다른 테이블에서 자동으로 옵니다. 양식에도 칸이 없습니다.

### 빼기 — Export

**테이블 하나** — Table Editor → 테이블 → 점 세 개 → **Export data as CSV**

**원하는 부분만** — SQL Editor에서 조회한 뒤 결과 표 오른쪽 위 **Export**.
뷰(`pl_monthly_v`, `customer_ltv_v` 등)는 Table Editor에 안 보이므로 이 방법을 씁니다.

```sql
select * from customer_ltv_v order by year, month;
```

### 수정 — Import 로는 안 됩니다

**CSV Import 는 줄을 새로 넣는 기능이고 기존 줄을 고치지 않습니다.**
수정한 CSV를 올리면 오류가 나거나 중복이 생깁니다.

| 상황 | 방법 |
|---|---|
| 새 달 추가 | CSV Import |
| 한두 칸 수정 | Table Editor에서 칸을 더블클릭해 직접 수정 |
| 한 달치 다시 넣기 | 그 달을 지우고 Import |

```sql
delete from sales_monthly where year = 2026 and month = 7;
```

**지우기 전에 반드시 Export 로 백업하세요.**

### 목록에 정해진 값을 늘리거나 줄일 때

**두 종류로 나뉩니다.**

| | 해당 칸 | 방법 |
|---|---|---|
| 자유롭게 바꿔도 됨 | 채널명 · 비용 항목명 · 부서명 · 거래처명 · 상품명 | 새 이름을 그냥 쓰면 됨. 단 매달 같은 표기를 유지 |
| **반드시 함께 고쳐야 함** | `source` · `category` · `cost_type` · 근속 `bucket` · 초과근무 `bucket` · `paid` | DB 제약과 대시보드 코드를 같이 수정 |

아래쪽 칸들은 DB에 `check` 제약이 걸려 있어 엑셀만 고치면 Import에서 막힙니다.

```
ERROR: new row violates check constraint "pl_cost_category_check"
```

**더 위험한 경우는 오류가 안 나는 쪽입니다.** 제약이 느슨한 칸에 새 값을 넣으면
저장은 되는데 대시보드가 그 값을 모르고 넘겨서, **숫자가 조용히 빠집니다.**

제약을 바꾸는 SQL은 이런 모양입니다.

```sql
alter table sales_pipeline drop constraint sales_pipeline_source_check;
alter table sales_pipeline add constraint sales_pipeline_source_check
  check (source in ('광고','직접 문의','소개·추천','영업 발굴','전시회'));
```

**제약만 고치고 끝나지 않습니다.** 새 값이 화면에 어떻게 표시될지, 집계에 어떻게 들어갈지
대시보드 코드도 함께 봐야 합니다. 값을 늘릴 때는 SQL과 파일을 한 번에 처리하세요.

엑셀 드롭다운 자체는 **데이터 → 데이터 유효성 검사 → 원본**에서 목록을 고치면 됩니다.

### 매달 흐름

1. **입력 전** — 건드릴 테이블을 Export 해서 `2026-07_백업` 폴더에 보관
2. **입력** — 엑셀에 새 달 줄 추가 → CSV Import
3. **확인** — `/exec` 입력 점검 화면에서 여섯 영역이 모두 초록인지 확인

무료 요금제에는 자동 백업이 없습니다. **직접 받아두는 것이 유일한 복구 수단**입니다.
분기에 한 번은 전체 테이블을 Export 해두세요.

## 12. 새 대시보드 만드는 순서

1. 어떤 숫자를 볼지 정한다
2. **이미 다른 테이블에 있는 값인지 먼저 확인한다** — 있으면 뷰로 가져온다
3. 테이블을 설계하고 SQL로 만든다 (RLS 포함)
4. `index.html`을 만든다
5. GitHub `+` → Create new file → `폴더명/index.html` → 붙여넣기 → Commit
6. `주소/폴더명` 으로 접속해 확인
7. 루트 `index.html`의 해당 카드를 활성화한다
8. `/exec` 신호등에 추가할지 검토한다
9. **`SCHEMA.md` 를 갱신한다** — 표·뷰 구조, 데이터 기간, 뷰가 하는 일

**SQL과 파일을 함께 바꿔야 할 때는 반드시 SQL 먼저, 파일 그다음입니다.** 단, 칼럼을 **삭제**할 때만 반대입니다. 파일을 먼저 올려 새 구조를 읽게 한 뒤 지웁니다.

## 13. 아직 안 정한 것

- 실제 숫자 전환 시점 — 정해지면 10번 절차를 먼저 밟을 것
- 매달 데이터를 넣는 방식 — Table Editor 직접 입력 vs CSV 가져오기
- **다음 달 매출 예측** — 지금 파이프라인은 지난 달 유입이 어떻게 끝났는지를 기록하므로 미래를 계산할 수 없음. `sales_pipeline_open`(현재 진행 중인 건의 단계별 잔량)을 따로 만들어야 가능
- 2025년 데이터가 영업·마케팅·손익·현금에만 있음 (고객·인사는 2026년만)
- 실제 회사의 유입 경로 구분이 지금 네 가지(광고 / 직접 문의 / 소개·추천 / 영업 발굴)와 맞는지 확인 필요
- 한 사람이 여러 번 문의할 때 몇 건으로 셀지 — 실제 데이터 전환 시 정해야 함

---

## 다른 AI에게 넘길 때

**네 가지 중 필요한 것만** 줍니다. 여섯 화면을 다 주면 무엇을 따라야 할지 헷갈립니다.

| 파일 | 왜 필요한가 | 언제 |
|---|---|---|
| `RULES.md` | 지켜야 할 규칙과 원칙 | 항상 |
| `SCHEMA.md` | 지금 표와 뷰가 어떻게 생겼는지 | 항상 |
| `pl/index.html` | 화면 패턴의 기본형 | 항상 |
| 작업할 영역의 `index.html` | 고칠 대상 | 기존 화면을 고칠 때만 |

**`RULES.md` 만으로는 부족합니다.** 규칙은 "어떻게 만들어야 하는가"이고
`SCHEMA.md` 는 "지금 어떻게 되어 있는가"입니다. 후자가 없으면
이미 있는 칼럼을 또 만들거나, 뷰가 계산하는 값을 저장하려 듭니다.

**파일은 GitHub에서 가져오는 게 확실합니다.**
저장소 → 해당 폴더 → 파일 클릭 → 오른쪽 위 복사 아이콘.
항상 최신본이라 예전 대화에서 찾아 헤맬 필요가 없습니다.

**`pl/index.html` 을 기본형으로 삼는 이유** — 뷰에서 데이터를 읽는 방식,
연도·월·단위 드롭다운, 카드 칩, 폭포 그래프, 반원 게이지, 표,
화면 폭에 따라 크기가 바뀌는 그래프까지 거의 모든 패턴이 이 파일 하나에 있습니다.

표가 중심인 화면을 만든다면 `marketing/index.html`,
여러 영역을 모으는 요약 화면이라면 `exec/index.html` 이 더 가깝습니다.

### 요청 문구

> 첨부한 `RULES.md` 와 `SCHEMA.md` 를 지켜서 [영역] 대시보드를 [만들어 / 고쳐]줘.
> `pl/index.html` 은 화면 패턴 참고용이고, [영역]/index.html 이 고칠 대상이야.
> 나는 코딩을 모르니 완성된 index.html 전체 코드를 한 번에 주고, 설명은 짧게.
> 파일 올리는 방법도 이름 바꾸기까지 포함해서 단계별로 알려줘.
> SQL이 필요하면 실행 순서까지 함께 알려줘.
> 보고 싶은 숫자는 [...] 이고, Supabase 테이블은 [있음 / 없어서 설계부터 필요] 상태야.

### 구조를 바꿨다면 SCHEMA.md 도 고칩니다

테이블이나 뷰를 바꾼 뒤에는 `schema-sql.txt` 를 실행해 현재 구조를 다시 뽑고
`SCHEMA.md` 를 갱신하세요. 이 문서가 실제와 어긋나면 다음 작업이 어긋납니다.
