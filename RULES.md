# 대시보드 프로젝트 규칙

> 새 대시보드를 만들거나 고칠 때 이 문서를 AI에게 먼저 붙여넣으세요.
> 마지막 수정: 2026-08-15 · 대시보드 6개 완성 시점

---

## 1. 이 프로젝트가 뭔지

사내 대시보드 6개를 웹페이지로 만들어 링크로 공유합니다.
숫자는 Supabase에서 고치면 웹페이지에 자동 반영됩니다.

```
GitHub (my-dashboard)  →  코드 보관
Supabase               →  숫자 보관
Vercel                 →  웹페이지로 공개 (GitHub에 올리면 1~2분 뒤 자동 반영)
```

## 2. 폴더와 주소

| 파일 위치 | 접속 주소 | 내용 |
|---|---|---|
| `index.html` | `/` | 6개 링크 + 영역별 핵심 숫자 |
| `sales/index.html` | `/sales` | 영업 |
| `marketing/index.html` | `/marketing` | 마케팅 |
| `customer/index.html` | `/customer` | 고객 |
| `pl/index.html` | `/pl` | 손익 |
| `hr/index.html` | `/hr` | 인사 |
| `exec/index.html` | `/exec` | 경영 (신호등 + 현금) |
| `RULES.md` | — | 이 문서 (웹에는 안 보임) |

**파일 이름은 항상 `index.html`.** 바뀌는 건 폴더 이름뿐입니다.

## 3. 절대 지켜야 할 것

- **파일 하나로 완성.** HTML·CSS·JS를 한 `index.html`에 다 넣습니다.
- **빌드·설치 과정 없음.** GitHub에 올리면 그걸로 끝이어야 합니다.
- **차트 라이브러리 금지.** 그래프는 순수 SVG 또는 HTML/CSS로 직접 그립니다.
- **`type="module"` 금지.** Supabase는 아래 방식으로만 불러옵니다.

```html
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script>
  const SUPABASE_URL = 'https://abgpkybrwtpzflkchchu.supabase.co';
  const SUPABASE_KEY = 'sb_publishable_4LUZJYwgyDYPb-EzzpDHRw_FwEUvJ1e';

  const { createClient } = supabase;
  const db = createClient(SUPABASE_URL, SUPABASE_KEY, {
    auth: { persistSession: false, autoRefreshToken: false }
  });
</script>
```

## 4. 이미 한 번 데인 것들 — 반복 금지

**변수 이름을 `URL`로 쓰면 안 됩니다.**
`const URL = '...'` 은 브라우저 내장 기능을 덮어써서 Supabase가 주소를 해석하지 못합니다.
증상: `Invalid supabaseUrl: Provided URL is malformed`
→ 반드시 `SUPABASE_URL`, `SUPABASE_KEY`로 씁니다.

**`persistSession: false`를 넣습니다.** 없으면 브라우저가 저장소 접근을 막아 경고가 쌓입니다.

**함수 안에서 화면 설정값과 같은 이름을 쓰면 안 됩니다.**
깔때기 함수 안에 `const unit = 계약단가` 를 만들었더니 화면 단위 설정값 `unit` 을 가려서
단위가 `undefined` 로 표시됐습니다. 지역 변수는 `unitPrice` 처럼 다른 이름을 씁니다.
같은 이유로 `unit`, `year`, `month`, `DATA` 는 함수 안에서 다시 선언하지 않습니다.

**RLS를 안 열면 데이터가 안 옵니다.** 테이블·뷰를 만들 때마다 읽기 정책을 함께 만듭니다.

**큰 수를 곱하면 정수 범위를 넘칩니다.**
증상: `ERROR: 22003: integer out of range`
→ 나누기가 뒤에 있어도 곱셈이 먼저 실행되므로 `::double precision`을 곱하기 **전에** 붙입니다.

**GitHub에서 덮어쓸 땐 해당 폴더 안으로 들어간 뒤 업로드합니다.**
루트에서 올리면 새 파일이 하나 더 생길 뿐 교체되지 않습니다.

**파일을 다운로드해서 올릴 땐 이름을 `index`로 바꿉니다.**
그리고 **다음 파일을 받기 전에 다운로드 폴더를 비웁니다.** 안 그러면 `index (1).html`이 되어 교체가 안 됩니다.

**새 폴더를 만들 때는 Upload가 아니라 Create new file.** 빈 폴더는 업로드로 만들 수 없습니다.

## 5. Supabase 규칙

**테이블 이름** — `영역_내용` 형식. 예: `sales_monthly`, `marketing_channel`

**금액 단위는 만원으로 통일.** 화면에서 만원/백만원/원으로 바꿔 보여주되 DB에는 항상 만원으로 저장합니다. 단, **CPC·CPA·CAC처럼 단가 성격의 값은 화면에서 항상 '원'으로** 표시합니다.

**단위를 어디에 쓸지는 숫자의 개수로 정합니다.**
값이 여러 개 나란히 놓이는 표·그래프는 **머리말이나 안내문에 한 번만** (`계약금액 (만원)`),
단독으로 나오는 카드 값은 **숫자에 직접** (`8,470만원`) 붙입니다.
표의 모든 값에 단위를 붙이면 자릿수 비교가 어려워집니다.

**시간은 `year`, `month` 숫자 칼럼으로.**
`sort_order`로 순번을 매기지 않습니다. 중간에 한 줄 끼워 넣을 때마다 뒤 번호를 전부 고쳐야 하기 때문입니다.
`sort_order`는 **순서에 의미가 있지만 시간이 아닌 것**에만 씁니다. 예: 영업 단계, 근속 구간

**아직 지나지 않은 기간은 비워둡니다(null).** 0을 넣으면 실적이 0인 것과 구분이 안 됩니다.

**새 테이블·뷰마다 읽기 권한을 엽니다.**

```sql
alter table 테이블명 enable row level security;
create policy "read" on 테이블명 for select to anon using (true);
```

**키는 공개돼도 안전합니다.** `sb_publishable_...` 키는 읽기 전용입니다.

## 6. 같은 숫자를 두 곳에 저장하지 않습니다

이 프로젝트에서 가장 중요한 규칙입니다.

**하나의 사실은 한 곳에만 저장하고, 다른 곳은 계산해서 가져옵니다.** 두 곳에 저장하면 언젠가 반드시 어긋나고, 그때 어느 쪽이 맞는지 알 수 없게 됩니다.

**계산되는 값은 저장하지 않습니다.** ROAS, 영업이익, 달성률, 전환율, LTV 모두 코드나 뷰에서 계산합니다.

**원본 데이터가 겹칠 때는 뷰(View)를 씁니다.** 뷰는 저장하지 않고 볼 때마다 원천에서 계산해 오는 통로입니다.

| 사실 | 원천 | 가져다 쓰는 곳 |
|---|---|---|
| 월 매출 | `sales_monthly` | `pl_monthly_v` |
| 월 광고비 | `marketing_channel` | `pl_cost_v` |
| 인건비 총액 | `pl_cost` | 인사 화면 |
| LTV | `customer_ltv_v` | 마케팅·고객 화면 |
| 광고·직접 문의 건수 | `marketing_channel` | `sales_pipeline_v` |

```sql
create or replace view 이름 with (security_invoker = on) as
select ...;
```

`security_invoker = on`을 빼면 RLS가 적용되지 않습니다.

**부서별 인건비처럼 배분이 필요한 경우, 금액이 아니라 비중(%)을 저장합니다.** 총액에 비중을 곱하면 합계가 항상 맞습니다.

**예외** — 회계 매출과 영업 매출처럼 실제로 다른 값이어야 하는 경우가 있습니다. 그때는 같은 이름으로 두 곳에 저장하지 말고 **다른 이름으로 둘 다 저장**하고, 그 차이 자체를 지표로 봅니다.

## 7. 디자인 규칙

- 흰 배경, 폰트는 Pretendard CDN
- 카드·패널: 테두리 `#E7EAF0`, 모서리 14px / 회색 글자 `#64748B`
- 휴대폰에서 안 깨져야 합니다

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.min.css">
```

**영역마다 포인트 색은 하나씩. 랜딩 페이지 카드 색이 기준입니다.** 색을 새로 고르지 말고 아래 표에서 가져다 씁니다.

| 영역 | 포인트 색 |
|---|---|
| 영업 `/sales` | `#1D4ED8` 파랑 |
| 마케팅 `/marketing` | `#6D45D9` 보라 |
| 고객 `/customer` | `#0E9E6E` 초록 |
| 손익 `/pl` | `#C2710A` 주황 |
| 인사 `/hr` | `#C43B69` 자홍 |
| 경영 `/exec` | `#41576B` 회청 |

강약이 필요하면 다른 색을 더하지 말고 **그 색의 농도**로 구분합니다.
`/exec` 신호등만 예외로 영역 6색을 그대로 씁니다.

**증감·달성 표시는 색만으로 하지 않습니다.** 포인트 색 배경 위에서는 회색 글자가 보이지 않고, 색을 구분하기 어려운 사람도 있습니다. **채움과 테두리로도 구분**되게 만듭니다.

| | 흰 배경 위 | 포인트 색 배경 위 |
|---|---|---|
| 좋음 | 포인트 색 채움 + 흰 글씨 | 흰색 채움 + 진한 포인트 색 글씨 |
| 나쁨 | 흰 배경 + 회색 테두리 | 투명 + 흰 테두리 + 흰 글씨 |
| 중립 | 연회색 채움 | 반투명 흰색 채움 |

**경고색은 따로 둡니다.** 기본은 `#B45309`, **포인트 색이 주황 계열인 손익 영역에서는 `#C0392B`**를 씁니다.
신호등 상태색은 정상 `#0E9E6E` · 주의 `#D97706` · 경고 `#C0392B`.

**조작 도구는 선택지 수에 따라 나눕니다.**

- 연도·월·단위처럼 **선택지가 많거나 늘어나는 것** → 드롭다운
- 기간(당월/누적), 표시 개수(5·10·20·전체)처럼 **선택지가 2~4개** → 버튼

## 8. 코드에 개수를 박아두지 않기

담당자가 3명이든 20명이든, 부서가 3개든 10개든 **화면이 데이터에 맞춰 늘어나야** 합니다.
Supabase에서 행을 추가하는 것만으로 반영되어야 하고, 그때 코드를 고쳐야 한다면 잘못 만든 것입니다.

## 9. 화면을 만들 때 지키는 것

**연도·월 선택은 모든 대시보드에 넣습니다.** 데이터가 있는 기간만 목록에 나오게 하고, 기본은 가장 최근입니다.

**영역마다 최신 달이 다를 수 있습니다.** 마케팅은 진행 중인 달이 있고 손익은 마감된 달까지입니다. 화면 위에 **기준 시점을 명시**합니다.

**숫자 옆에 판단 기준을 같이 둡니다.** "ROAS 464%"만으로는 좋은지 알 수 없습니다. 목표, 전월, 전년, 손익분기 같은 비교 대상을 함께 보여줍니다.

**낮을수록 좋은 지표는 달성률로 표시하지 않습니다.**
인건비 비중을 `목표 ÷ 실적`으로 계산하면 195% 같은 값이 나와 오해를 부릅니다.
→ **실제 값을 그대로 크게 보여주고**, 막대는 기준선 대비 채움으로, 설명에 "낮을수록 좋음 · 기준 35%"를 붙입니다.

**데이터를 못 불러오면** 화면을 비우지 말고 주황색 안내줄과 함께 예시 값을 보여주거나, 최소한 원인을 안내합니다.

**검산 장치를 넣습니다.** 현금은 `월초 + 유입 − 유출 = 월말`, 부서는 `비중 합 = 100%`, 인원은 `부서 합 = 전사`. 안 맞으면 화면에 경고를 띄웁니다.

**두 화면의 숫자가 다르면 억지로 맞추지 말고, 왜 다른지 먼저 봅니다.**
마케팅 전환 493건과 영업 문의 1,036건이 달랐던 것은 오류가 아니라
**광고를 거치지 않은 유입이 따로 있었기 때문**이었습니다.
`sales_pipeline` 에 `source`(광고 / 직접 문의 / 소개·추천 / 영업 발굴)를 넣어 나누자
광고 의존도와 경로별 계약률이라는 새 지표가 생겼습니다.
숫자가 안 맞을 때는 **정의를 통일하기 전에 쪼갤 수 있는지** 먼저 검토하세요.

## 10. 지금 들어 있는 숫자는 전부 샘플입니다

화면 확인용으로 만든 가짜 숫자이고, **일부는 SQL이 자동 생성**합니다.
`sales_rank`, `marketing_channel`, `customer_cohort`, `hr_team`이 그렇습니다.

**실제 숫자를 손으로 넣기 시작한 뒤에는 그 SQL을 다시 실행하면 안 됩니다.**
첫 줄이 `drop table`이라 입력한 내용이 전부 지워집니다.

**실제 데이터로 넘어갈 때 할 일**

1. 지금 데이터를 CSV로 내보내 보관 — Table Editor → 점 세 개 → Export
2. 자동 생성 SQL은 더 이상 쓰지 않기로 하고 따로 보관
3. 실제 실적을 넣는 방식 결정 (직접 입력 / CSV 가져오기)

**숫자를 크게 고치기 전에는 CSV로 한 번 내보내 두세요.** 되돌릴 방법이 그것뿐입니다.

**규모가 서로 맞는지 확인하세요.** 매출 8,470만원 회사에 인원 24명을 넣으면 인건비만 매출의 85%가 되어 이익이 나올 수 없습니다. 지금 샘플은 **5명 규모**로 맞춰져 있습니다.

## 11. 새 대시보드 만드는 순서

1. 어떤 숫자를 볼지 정한다
2. **이미 다른 테이블에 있는 값인지 먼저 확인한다** — 있으면 뷰로 가져온다
3. 테이블을 설계하고 SQL로 만든다 (RLS 포함)
4. `index.html`을 만든다
5. GitHub `+` → Create new file → `폴더명/index.html` → 붙여넣기 → Commit
6. `주소/폴더명` 으로 접속해 확인
7. 루트 `index.html`의 해당 카드를 활성화한다
8. `/exec` 신호등에 추가할지 검토한다

**SQL과 파일을 함께 바꿔야 할 때는 반드시 SQL 먼저, 파일 그다음입니다.** 단, 칼럼을 **삭제**할 때만 반대입니다. 파일을 먼저 올려 새 구조를 읽게 한 뒤 지웁니다.

## 12. 아직 안 정한 것

- 실제 숫자 전환 시점 — 정해지면 10번 절차를 먼저 밟을 것
- 매달 데이터를 넣는 방식 — Table Editor 직접 입력 vs CSV 가져오기
- **다음 달 매출 예측** — 지금 파이프라인은 지난 달 유입이 어떻게 끝났는지를 기록하므로 미래를 계산할 수 없음. `sales_pipeline_open`(현재 진행 중인 건의 단계별 잔량)을 따로 만들어야 가능
- 2025년 데이터가 영업·마케팅·손익·현금에만 있음 (고객·인사는 2026년만)
- 실제 회사의 유입 경로 구분이 지금 네 가지(광고 / 직접 문의 / 소개·추천 / 영업 발굴)와 맞는지 확인 필요
- 한 사람이 여러 번 문의할 때 몇 건으로 셀지 — 실제 데이터 전환 시 정해야 함

---

## 다른 AI에게 넘길 때

**이 문서 + 참고용 `index.html` 한 개**를 함께 줍니다.
여섯 개를 다 주면 무엇을 따라야 할지 헷갈리므로 **만들려는 것과 가장 비슷한 하나만** 고릅니다.

| 만들려는 것 | 함께 줄 파일 | 이유 |
|---|---|---|
| 새 영역 대시보드 | `pl/index.html` | 카드·그래프·표·게이지가 다 들어 있어 참고 폭이 가장 넓음 |
| 표가 중심인 화면 | `marketing/index.html` | 표 + 행 클릭 상세 + 용어 설명 구조 |
| 여러 영역을 모으는 요약 화면 | `exec/index.html` | 여러 테이블을 합쳐 계산하는 방식 |
| 기존 화면 수정 | 그 화면 파일 | 고칠 대상 그 자체 |

**어느 쪽인지 모르겠으면 `pl/index.html`을 주세요.** 뷰에서 데이터를 읽는 방식, 연도·월·단위 드롭다운,
카드 칩, 폭포 그래프, 반원 게이지, 표, 화면 폭에 따라 크기가 바뀌는 그래프까지
이 프로젝트의 거의 모든 패턴이 이 파일 하나에 들어 있습니다.

**파일은 GitHub에서 가져오는 게 확실합니다.**
저장소 → 해당 폴더 → `index.html` 클릭 → 오른쪽 위 복사 아이콘 → 붙여넣기.
항상 최신본이라 예전 대화에서 파일을 찾아 헤맬 필요가 없습니다.

### 요청 문구

> 첨부한 `RULES.md`를 지켜서 [영역] 대시보드를 만들어줘.
> `pl/index.html`을 참고용으로 같이 줄게. 이 파일의 구조와 스타일을 따라줘.
> 나는 코딩을 모르니 완성된 index.html 전체 코드를 한 번에 주고, 설명은 짧게.
> 파일을 올리는 방법도 이름 바꾸기까지 포함해서 단계별로 알려줘.
> 보고 싶은 숫자는 [...] 이고, Supabase 테이블은 [있음 / 없어서 설계부터 필요] 상태야.
